const lodash = require("lodash");
const express = require("express");

const number = [1, 2, 3, 4, 5, 6];
console.log(lodash.reverse(number));

//Local and Global packages

//Local package
const app = express();

//Global package
